"""
The State class inherits from `BaseModel`
and has a `name` attribute.
"""

from models.base_model import BaseModel as Bsss


class State(Bsss):
    """The class `State` inherits from
    `BaseModel` and has a `name` attribute."""

    name = ""
